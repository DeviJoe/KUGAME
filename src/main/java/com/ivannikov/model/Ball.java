package com.ivannikov.model;

import java.awt.image.BufferedImage;

public class Ball extends ColourEntity {

    public Ball(BufferedImage texture, Point coordinate, Colour colour) {
        super(texture, coordinate, colour);
    }
}
