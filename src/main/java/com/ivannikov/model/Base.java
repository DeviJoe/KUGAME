package com.ivannikov.model;

import java.awt.image.BufferedImage;

public class Base extends ColourEntity {


    public Base(BufferedImage texture, Point coordinate, Colour colour) {
        super(texture, coordinate, colour);
    }
}
