package com.ivannikov.model;

import java.awt.image.BufferedImage;

public class Wall extends Entity {

    public Wall(BufferedImage texture, Point coordinate) {
        super(texture, coordinate);
    }
}
